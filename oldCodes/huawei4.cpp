#include <bits/stdc++.h>
using namespace std;

int main() {
    int q;
    cin >> q;
    vector<vector<int>> lock(q, vector<int>(2));
    for (int i = 0; i < q; i++) {
        cin >> lock[i][0] >> lock[i][1];
    }

}

